'use strict';

import React from 'react';
import TeamIcon from './TeamIconWrapperComponent';



require('styles//TeamIconWrapper.css');

export default class TeamIconWrapper extends React.Component {
  render() {
    return (
      <ul className="team-icon-wrapper">Text</ul>
    );
  }
}

TeamIconWrapper.displayName = 'TeamIconWrapperComponent';

// Uncomment properties you need
// TeamIconWrapperComponent.propTypes = {};
// TeamIconWrapperComponent.defaultProps = {};


